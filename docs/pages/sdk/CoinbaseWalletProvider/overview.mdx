# Overview

CoinbaseWalletProvider is an [Ethereum Javascript provider](https://eips.ethereum.org/EIPS/eip-1193) provider.
Most RPC requests received are passed directly to the wallet, creating a popup window, in the case of Smart Wallet.
In this section we document the known RPC requests which the SDK is intended to service.